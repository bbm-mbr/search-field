fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7,<2.11
pydantic-core>=2.18,<2.28
httpx>=0.27
python-dotenv>=1.0
# LLM client (OpenAI SDK — used only for Azure OpenAI embeddings via AzureOpenAI class)
openai>=1.40
# Free, key-less web search (DuckDuckGo)
ddgs>=9.0
# RSS / news (Google News RSS, GDELT) parsing
feedparser>=6.0
# RAG over the Search-Field playbook
chromadb>=0.5
pypdf>=4.0
# Local embeddings fallback if Azure endpoint not configured
sentence-transformers>=3.0
# PPTX export
python-pptx>=0.6.23
# Tests
pytest>=8.0
pytest-asyncio>=0.23
